name: pocketbizz
description: PocketBizz - SME Management App
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.3.4
  
  # UI
  cupertino_icons: ^1.0.2
  
  # State Management (choose one)
  # provider: ^6.1.1
  # riverpod: ^2.4.9
  # bloc: ^8.1.3
  
  # Utilities
  intl: ^0.19.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

